using Vintagestory.API.Common;

[assembly: ModInfo(
    "Komet Baseline",
    "kometbase",
    Version = "1.1.0",
    Side = "Client",
    RequiredOnClient = false,
    RequiredOnServer = false,
    Description = "Measurement only: the same performance HUD as Komet with none of its optimisations, so vanilla can be compared against it. Stands down automatically when Komet is enabled.",
    Authors = new[] { "xToast" })]
